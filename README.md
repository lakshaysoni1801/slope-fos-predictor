# slope-fos-predictor
ML tool predicting a slope's Factor of Safety (FOS) and Stable/Unstable status from geotechnical parameters (cohesion, friction angle, slope geometry). Includes a Random Forest regression/classification notebook and a standalone, no-install HTML web tool for real-time predictions.
